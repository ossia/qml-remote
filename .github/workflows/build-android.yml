name: Build Android app

on: push

jobs:
  android:
    name: Android build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      # Host Qt: androiddeployqt + qt-cmake host tools. Installed alongside the
      # android Qt so qt-cmake auto-detects QT_HOST_PATH from the sibling. The
      # host version must match the Android Qt version (6.12.0).
      - name: Install Qt (host)
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.12.0'
          host: linux
          target: desktop
          arch: linux_gcc_64
          modules: qtwebsockets
          dir: ${{ runner.temp }}/qt

      # Qt 6.8+ publishes Android under the unified "all_os" host (the "linux"
      # host only goes up to 6.7.x).
      - name: Install Qt (android arm64)
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.12.0'
          host: all_os
          target: android
          arch: android_arm64_v8a
          modules: qtwebsockets
          dir: ${{ runner.temp }}/qt

      # ubuntu-latest ships an Android SDK/NDK; Qt 6.12 expects NDK r27c
      # (27.2.12479018). Pin it so the build is reproducible.
      - name: Install Android NDK 27.2.12479018
        run: |
          "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" --install "ndk;27.2.12479018"
          echo "ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/27.2.12479018" >> "$GITHUB_ENV"

      - name: Build
        run: |
          # $QT_ROOT_DIR is set by install-qt-action to the last (android) install.
          "$QT_ROOT_DIR/bin/qt-cmake" -G Ninja -S src -B build-android \
            -DCMAKE_BUILD_TYPE=Release \
            -DANDROID_SDK_ROOT="$ANDROID_SDK_ROOT" \
            -DANDROID_NDK_ROOT="$ANDROID_NDK_ROOT" \
            -DQT_ANDROID_BUILD_ALL_ABIS=OFF
          cmake --build build-android
          cmake --build build-android --target apk

      # Qt 6 + CMake emits the APK under the gradle output tree.
      - name: Collect APK
        run: |
          APK=$(find build-android -name '*.apk' | head -n1)
          echo "Found APK: $APK"
          cp "$APK" ossia-remote.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Android build
          path: ossia-remote.apk

      - name: Upload release
        if: startsWith(github.ref, 'refs/tags')
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ossia-remote.apk
          tag: ${{ github.ref }}
          overwrite: true
          file_glob: true
